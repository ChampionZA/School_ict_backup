#include <stdio.h>
#include <stdlib.h>

int main() {
    
    int size;
    printf("How big is u're juicer: ");
    scanf("%d", &size);
    printf("Your jucier is %d digits big!!!", size);

    return 0;
}